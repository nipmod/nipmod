interface:
  display_name: "Nipmod Package Decision"
  short_description: "Codex package preflight and install guard"
  icon_small: "assets/nipmod-logo.png"
  icon_large: "assets/nipmod-logo.png"
  brand_color: "#E5392B"
  default_prompt: "Use Nipmod codex_preflight for package recommendations and install_guard before dependency writes. Show trust/risk, approval packet and install boundary, then wait for approval before changing dependencies."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "nipmod"
      description: "Nipmod package decision MCP server"
      transport: "streamable_http"
      url: "https://nipmod.com/api/mcp"
